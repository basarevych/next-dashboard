import { withStyles } from "@material-ui/styles";
import MessageModalComponent, { styles } from "./MessageModal";

const MessageModal = withStyles(styles)(MessageModalComponent);

export default MessageModal;
